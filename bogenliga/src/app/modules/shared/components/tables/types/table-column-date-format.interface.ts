export interface TableColumnDateFormat {
  de: {
    language: string,
    dateFormat: string;
    timeFormat?: string;
  };
  en: {
    language: string,
    dateFormat: string;
    timeFormat?: string;
  };
}
